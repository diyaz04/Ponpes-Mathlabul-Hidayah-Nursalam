# =========================================================================
# SERVER-SIDE SECRETS (Never commit actual keys here!)
# =========================================================================

# GEMINI_API_KEY: Required for Gemini AI assistant.
# Configured securely in the AI Studio Settings / Secrets panel.
GEMINI_API_KEY=""

# APP_URL: The URL where the application or API is accessible.
APP_URL=""

# MIDTRANS GATEWAY INTEGRATION (For QRIS + Virtual Account payments)
MIDTRANS_SERVER_KEY=""
MIDTRANS_CLIENT_KEY=""
MIDTRANS_IS_PRODUCTION="false"

# WEB PUSH API & PWA (For push notifications to parent devices)
WEB_PUSH_CONTACT_MAIL="mailto:admin@mathlabulhidayah.sch.id"
WEB_PUSH_PUBLIC_VAPID_KEY=""
WEB_PUSH_PRIVATE_VAPID_KEY=""

# =========================================================================
# CLIENT-SIDE PUBLIC VARIABLES (Exposed to Vite frontend)
# =========================================================================

# CLOUDINARY INTEGRATION (For compressed image uploads)
VITE_CLOUDINARY_CLOUD_NAME=""
VITE_CLOUDINARY_UPLOAD_PRESET=""

# SUPABASE DEPLOYMENT CONFIG (Auth & PostgreSQL storage)
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""
